# 概要
- グループワークの作業内容

## 期間
- 2026/03/22～2026/03/28

## 作業内容
1. GitHubの設定変更
- 菊地さん、安田さんをCollaboratorsに追加して、Privateに変更

2. Docker Composeの利用
- `Docker+VSCodeSetup2.md` にまとめ

3. AIを利用する場合の運用（ファイルのアクセス制御など）
- `.claude\settings.json` でファイルアクセスを制御
- `*.env` に対する読み取り、書き込みを拒否

4. claude codeをDockerのコンテナで利用
- `devcontainer.json` に以下を追加

```
  "features": {
    "ghcr.io/anthropics/devcontainer-features/claude-code:1": {}
  },
  "mounts": [
    "source=${localEnv:HOME}/.claude,target=/root/.claude,type=bind,consistency=cached"
  ],
```

5. claude codeでQA管理者のsubagentを作成
- 使い方：

```
# テスト全体を実行してレポートして
qa-agent を使って全テストを実行してエラーを報告して

# 新しく作ったコンポーネントのテストを生成して
qa-agent を使って src/components/UserCard.tsx のテストを生成して

# カバレッジを確認して
qa-agent を使ってカバレッジを計測して改善点を教えて

# リグレッションチェック
qa-agent を使って最新の変更でリグレッションが起きていないか確認して
```

6. subagentでVitestとPlaywrightのテストを作成、実行
- レポートは `test-results.md` に出力

## 継続課題
1. claude codeの使用
- subagentの学習
- skillsの学習


