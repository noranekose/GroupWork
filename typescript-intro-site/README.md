# TypeScript 入門サイト

ブラウザ上で TypeScript を編集・実行して学べる学習用サイトです（Vite + React + TypeScript）。

## 起動方法

```bash
cd typescript-intro-site
npm install
npm run dev
```

ブラウザで表示された URL（通常 `http://localhost:5173`）を開きます。

## ビルド

```bash
npm run build
npm run preview
```
