# テスト結果レポート

実施日時: 2026-03-28

## サマリー

| 項目 | 件数 |
|------|------|
| テストファイル | 3 |
| 合格 | **50** |
| 失敗 | **0** |
| 実行時間 | 約4.25秒 |

**結果: 全テスト合格**

---

## テストファイル別結果

### 1. `src/lib/normalizeOutput.test.ts` — 19件 全合格

| describe | テスト名 | 結果 |
|----------|----------|------|
| 基本的な動作 | 通常の文字列をそのまま返す | ✅ |
| 基本的な動作 | 空文字列を空文字列として返す | ✅ |
| 基本的な動作 | 空白のみの文字列を空文字列にする | ✅ |
| 改行コードの正規化 | CRLF（Windows改行）をLFに変換する | ✅ |
| 改行コードの正規化 | 複数のCRLFをすべてLFに変換する | ✅ |
| 改行コードの正規化 | LFのみの改行はそのまま保持する | ✅ |
| 行末の空白トリム | 各行末のスペースを除去する | ✅ |
| 行末の空白トリム | 各行末のタブを除去する | ✅ |
| 行末の空白トリム | 行頭の空白は trim() によって先頭行の先頭空白が除去される | ✅ |
| 前後のトリム | 文字列先頭の改行を除去する | ✅ |
| 前後のトリム | 文字列末尾の改行を除去する | ✅ |
| 前後のトリム | 前後の空白と改行を除去する | ✅ |
| 組み合わせ | CRLFと行末スペースと前後トリムを同時に処理する | ✅ |
| 組み合わせ | 実際のレッスン出力形式を正規化する（Hello, TypeScript!） | ✅ |
| 組み合わせ | 複数行出力の正規化（typesレッスンの期待出力） | ✅ |
| 境界値テスト | 改行のみの文字列を空文字列にする | ✅ |
| 境界値テスト | CRLFのみの文字列を空文字列にする | ✅ |
| 境界値テスト | 1文字の文字列をそのまま返す | ✅ |
| 境界値テスト | 日本語文字列を正しく処理する | ✅ |

---

### 2. `src/lib/runTypeScript.test.ts` — 11件 全合格

| describe | テスト名 | 結果 |
|----------|----------|------|
| 正常なTypeScriptコードのトランスパイル | 型注釈付きのコードを正常にトランスパイルする | ✅ |
| 正常なTypeScriptコードのトランスパイル | number型の変数を正常にトランスパイルする | ✅ |
| 正常なTypeScriptコードのトランスパイル | インターフェースを正常にトランスパイルする | ✅ |
| 正常なTypeScriptコードのトランスパイル | ユニオン型を正常にトランスパイルする | ✅ |
| 正常なTypeScriptコードのトランスパイル | 型注釈がトランスパイル後に除去される | ✅ |
| 型エラーの検出 | 型不一致エラーを検出する（number に string を代入） | ✅ |
| 型エラーの検出 | 型エラーメッセージが文字列として取得できる | ✅ |
| 型エラーの検出 | 構文エラーはトランスパイル時に検出される | ✅ |
| NO_LOG_PLACEHOLDER 定数 | NO_LOG_PLACEHOLDER が正しい値で export されている | ✅ |
| runTypeScript のトランスパイルエラー検出 | 型エラーのあるコードは success: false を返す | ✅ |
| runTypeScript のトランスパイルエラー検出 | 構文エラーのあるコードは success: false を返す | ✅ |

---

### 3. `src/data/lessons.test.ts` — 20件 全合格

| describe | テスト名 | 結果 |
|----------|----------|------|
| レッスン配列の基本構造 | lessons が配列として export されている | ✅ |
| レッスン配列の基本構造 | レッスンが1件以上存在する | ✅ |
| レッスン配列の基本構造 | 現在5件のレッスンが定義されている | ✅ |
| 各レッスンの必須フィールド検証 | すべてのレッスンに id が存在する | ✅ |
| 各レッスンの必須フィールド検証 | すべてのレッスンに title が存在する | ✅ |
| 各レッスンの必須フィールド検証 | すべてのレッスンに description が存在する | ✅ |
| 各レッスンの必須フィールド検証 | すべてのレッスンに initialCode が存在する（空文字も可） | ✅ |
| 各レッスンの必須フィールド検証 | すべてのレッスンに expectedOutput が存在する | ✅ |
| id の一意性検証 | すべてのレッスン id が一意である | ✅ |
| オプションフィールドの型検証 | hint が定義されている場合は string 型である | ✅ |
| オプションフィールドの型検証 | hint がない場合は undefined である | ✅ |
| 個別レッスンの内容検証 | hello レッスンが正しく定義されている | ✅ |
| 個別レッスンの内容検証 | types レッスンが正しく定義されている | ✅ |
| 個別レッスンの内容検証 | interface レッスンが正しく定義されている | ✅ |
| 個別レッスンの内容検証 | function レッスンが正しく定義されている | ✅ |
| 個別レッスンの内容検証 | union レッスンが正しく定義されている | ✅ |
| Lesson 型との整合性検証 | lessons の各要素が Lesson 型の必須キーをすべて持つ | ✅ |
| Lesson 型との整合性検証 | lessons の各要素に Lesson 型に存在しないキーがない | ✅ |
| expectedOutput の整合性検証 | すべての expectedOutput が空でない | ✅ |
| expectedOutput の整合性検証 | hint が存在するレッスンでは hint に console.log が含まれる | ✅ |

---

## テスト実行中に発見した仕様の注意点

---

---

# Playwright E2E テスト結果レポート

実施日時: 2026-03-28

## サマリー

| 項目 | 件数 |
|------|------|
| テストファイル | 1 (`e2e/app.spec.ts`) |
| テストケース合計 | 26 |
| 合格 | **25** |
| スキップ | **1**（Monaco Editor 入力テスト） |
| 失敗 | **0** |
| 実行時間 | 約26.3秒 |
| ブラウザ | Chromium（ヘッドレス） |

**結果: 全テスト合格（スキップ1件は仕様による意図的スキップ）**

---

## テストファイル別結果

### `e2e/app.spec.ts` — 25件合格 / 1件スキップ

#### フロー1: ページ初期表示（6件）

| テスト名 | 結果 |
|----------|------|
| ページタイトル「TypeScript 入門」が表示される | ✅ |
| 最初のレッスン「1. はじめての TypeScript」がアクティブ状態で表示される | ✅ |
| サイドバーに5件のレッスンボタンが表示される | ✅ |
| 「実行する」ボタンが表示される | ✅ |
| 「判定する」ボタンが表示される | ✅ |
| 「初期コードに戻す」ボタンが表示される | ✅ |

#### フロー2: レッスン切り替え（4件）

| テスト名 | 結果 |
|----------|------|
| サイドバーの「2. 基本的な型」をクリックするとレッスンが切り替わる | ✅ |
| 切り替え後、説明文が更新される | ✅ |
| レッスン切り替え後、出力パネルがリセットされプレースホルダーが表示される | ✅ |
| すべての5レッスン間を順番に切り替えられる | ✅ |

#### フロー3: ヒント表示（3件）

| テスト名 | 結果 |
|----------|------|
| details.hint の summary をクリックするとヒントが展開される | ✅ |
| ヒントを展開するとコードが表示される | ✅ |
| ヒントを展開後にもう一度クリックすると閉じる | ✅ |

#### フロー4: コード実行（3件、うち1件スキップ）

| テスト名 | 結果 |
|----------|------|
| Monaco Editor へのコード入力テストは SKIP（WebComponent のため入力不可） | ⏭ SKIP |
| 「実行する」ボタンがクリック可能である | ✅ |
| 「実行する」ボタンをクリックすると処理が実行される（エラーにならない） | ✅ |

#### フロー5: 出力パネルの初期状態確認（3件）

| テスト名 | 結果 |
|----------|------|
| 「実行する」を押す前はプレースホルダーが表示される | ✅ |
| 出力パネルに「実行結果」見出しが表示される | ✅ |
| 出力パネルが aria-live="polite" を持つ | ✅ |

#### フロー6: 初期コードに戻す（3件）

| テスト名 | 結果 |
|----------|------|
| 「初期コードに戻す」ボタンをクリックしてもエラーにならない | ✅ |
| 「初期コードに戻す」をクリックすると出力パネルがリセットされる | ✅ |
| レッスン切り替え後に「初期コードに戻す」をクリックしてもエラーにならない | ✅ |

#### ボーナス: ページ構造とアクセシビリティ（4件）

| テスト名 | 結果 |
|----------|------|
| header 要素が存在する | ✅ |
| footer 要素が存在する | ✅ |
| Monaco エディタコンテナが aria-label を持つ | ✅ |
| ナビゲーションが aria-label="レッスン一覧" を持つ | ✅ |

---

## テスト実行中に発見した仕様・品質の注意点

### Monaco Editor のテスト制限（重大度：中）

**詳細:**
Monaco Editor は WebComponent として描画されるため、`page.fill()` や `page.type()` によるコード入力テストが利用できない。
そのため「ヒント通りのコードを入力して判定する」といったエンドツーエンドのハッピーパステストが実施不可能。

**影響:** 合格・不合格モーダルの表示、実行後の出力内容確認など、コード入力を前提とするシナリオをカバーできない。

**推奨対応:**
- Playwright の `page.keyboard.type()` と Monaco の API を組み合わせる（クリックでフォーカス後にキーボード入力）
- または、テスト専用の `data-testid` 属性付きシンプルエディタモードを用意する

### スキップテスト（重大度：低）

| スキップテスト名 | スキップ理由 |
|-----------------|-------------|
| Monaco Editor へのコード入力テストは SKIP | `page.fill()` / `page.type()` が Monaco WebComponent に対応しない |

---

### normalizeOutputForJudge の挙動（重大度：低）

**詳細:**
`normalizeOutputForJudge` は `.trim()` を最後に適用するため、入力文字列の**先頭が空白文字で始まる場合、その空白も除去**される。

```
入力: '  indented\n  also indented'
出力: 'indented\n  also indented'  // 先頭行の先頭空白が除去される
```

**影響:** 現状のレッスン（`expectedOutput`）はすべてインデントなしの出力を期待しているため実害はない。ただし、今後インデントを含む `expectedOutput` を持つレッスンを追加する場合は注意が必要。

### ts.transpileModule の isolatedModules 制限（重大度：低）

**詳細:**
`runTypeScript.ts` では `isolatedModules: true` でトランスパイルしているため、一部の型エラー（例: string 型の存在しないメソッド呼び出し）はトランスパイル時に**検出されない**。構文エラーや `import type` 違反などは検出される。

**影響:** 学習用サンドボックスとしての用途では許容範囲内。実行時エラーはサンドボックス側で捕捉されるため、致命的な問題にはならない。

---

## テスト対象外（カバレッジ除外）の機能

| 機能 | 除外理由 |
|------|----------|
| `executeInSandbox` | iframe / postMessage が必要なブラウザ固有の非同期通信 |
| `ensureSandboxFrame` | DOM への iframe 追加と非同期メッセージ待機が必要 |
| `installMessageListener` | window のグローバルイベントリスナー登録 |
| `getSandboxHtml` | HTML 文字列生成（副作用なし・単体テスト可能だが優先度低） |
| `cleanupSandbox` | モジュールスコープの変数操作（副作用あり） |
