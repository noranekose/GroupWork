# 報告レポート：Cursor Agent 依頼内容と TypeScript 入門サイトの対応

## 1. 概要

本レポートは、グループ作業用の **Cursor Agent 依頼フォーマット**（3 版）に記載された要件と、実装済みの **`typescript-intro-site`** の内容を整理し、目的・範囲・達成状況を報告するものです。

**参照ファイル**

- `CursorAgentフォーマット.md`（第 1 版・全体像）
- `CursorAgentフォーマット_v2.md`（第 2 版・問題文・ヒント・初期コード）
- `CursorAgentフォーマット_v3.md`（第 3 版・判定ボタン・合否 UI）
- ※ リポジトリ内に `CursorAgentフォーマット._v2md` という名前のファイルは見当たらないため、同等内容と推定される `CursorAgentフォーマット_v2.md` を用いた。

**対象プロジェクト**

- `typescript-intro-site/`（Vite + React + TypeScript）

---

## 2. Cursor Agent フォーマット各版の要約

### 2.1 第 1 版（`CursorAgentフォーマット.md`）

| 項目 | 内容 |
|------|------|
| やりたいこと | TypeScript 入門サイトの作成 |
| 目的 | 初心者がコード入力・実行しながら学べるサイト |
| 作業範囲 | 問題画面（問題文 / コード入力 / 実行結果）、実行ボタン、解答ボタンによる合否、「おめでとう！！」／「残念！！」表示 |
| 使用技術 | React / TypeScript / Vite など |
| 出力形式 | 実装方針 → 変更ファイル → コード → 注意点 |

### 2.2 第 2 版（`CursorAgentフォーマット_v2.md`）

| 項目 | 内容 |
|------|------|
| やりたいこと | 問題文の実装 |
| 目的 | 考えてコードを書けるようにする |
| 作業範囲 | 問題エリアの文言変更、第 1 問を「Hello, TypeScript!」出力に、ヒント欄にサンプルコード、他レッスンも同様、テキストエリアは空または穴抜きが望ましい |

### 2.3 第 3 版（`CursorAgentフォーマット_v3.md`）

| 項目 | 内容 |
|------|------|
| やりたいこと | 判定ボタンの実装 |
| 目的 | 実行結果が期待値どおりか検証したい |
| 作業範囲 | 実行結果エリア付近に判定ボタン、押下でチェック、OK なら「合格！」と閉じるボタン、NG なら「不合格！」と初期コード状態への復帰 |

**フォーマット共通**

- 「条件」「関連ファイル」はプレースホルダが多く、詳細仕様は第 1〜3 版の箇条書きが実質的な依頼内容となっている。
- Agent への指示の出し方として「実装方針 → ファイル → コード → 注意点」が統一されている。

---

## 3. `typescript-intro-site` の内容

### 3.1 プロジェクト概要（README・`package.json`）

- **概要**: ブラウザ上で TypeScript を編集・実行して学ぶ学習用サイト。
- **スタック**: React 19、TypeScript 5.9 系、Vite 8、ESLint。
- **起動**: `npm install` → `npm run dev`（一般に `http://localhost:5173`）。

### 3.2 画面構成・機能（`App.tsx` ほか）

実装は第 1 版の「問題 / エディタ / 実行結果」レイアウトに沿っている。

| 要素 | 実装 |
|------|------|
| レッスン一覧 | サイドバーで複数レッスンを切り替え可能 |
| 問題エリア | タイトル・説明、`details` によるヒント（折りたたみ） |
| コード入力 | `textarea`、プレースホルダで入力を促す |
| 実行 | 「実行する」ボタン → エラーは赤、`console.log` 集成功は緑表示 |
| 初期化 | 「初期コードに戻す」、`initialCode` への復帰 |
| 判定 | 「実行結果」見出し横の「判定する」ボタン（第 3 版の「実行結果の位置に判定ボタン」に相当） |
| 合格時 | モーダルで「合格！」、`OK表示を閉じる` |
| 不合格時 | モーダルで「不合格！」、**コード・出力・エラーを初期状態に戻す**、閉じる |

文言は第 1 版の「おめでとう！！」／「残念！！」ではなく **「合格！」**／**「不合格！」** だが、第 3 版の「合格！」／「不合格！」とは一致する。

### 3.3 レッスンデータ（`src/data/lessons.ts`）

- 型 `Lesson`: `description`（問題文相当）、`hint`（任意）、`initialCode`、`expectedOutput`（合格判定用）。
- 現状 **5 レッスン**（Hello TypeScript、基本型、interface、関数の型、ユニオン型）。
- **第 2 版要件**: 第 1 問は「Hello, TypeScript!」、ヒントにサンプルコード、**`initialCode` は空文字** → **満たしている**（他レッスンも同パターン）。

### 3.4 実行エンジン（`src/lib/runTypeScript.ts`）

- TypeScript コンパイラ API で `transpileModule`、診断エラー時は成功とせずメッセージ返却。
- トランスパイル後の JS を `new Function` で実行し、`console.log` / `error` / `warn` を捕捉して 1 本の文字列にまとめる。
- 出力が無い場合はプレースホルダ文を返す（判定側でこの状態は不合格扱い）。

### 3.5 判定の正規化（`src/lib/normalizeOutput.ts`）

- 改行コード・行末空白・前後空白を揃えて期待値と比較（環境差による誤不合格を抑制）。

### 3.6 運用上の注意（フッター記載）

- 学習用サンドボックスであり、ネットワーク・ファイルにはアクセスしない旨をユーザーに明示。

---

## 4. 要件と実装の対応表

| フォーマット要件 | 対応状況 | メモ |
|------------------|----------|------|
| 問題 / 入力 / 結果の 3 領域 | ○ | `lesson-panel` / `editor-panel` / `output-panel` |
| コード実行 | ○ | `runTypeScript`、動的 import |
| 合否判定 | ○ | `expectedOutput` と正規化後の実出力を比較 |
| 合格メッセージ・閉じる | ○ | 「合格！」モーダル、`OK表示を閉じる` |
| 不合格メッセージ・初期状態へ | ○ | 「不合格！」、エラー・出力なし・不一致で `resetLessonToInitial` |
| 問題文・ヒント・空の初期コード | ○ | `lessons.ts` で管理 |
| 第 1 版の「おめでとう！！」／「残念！！」 | △ | 文言は第 3 版に合わせた「合格！」／「不合格！」 |
| 解答ボタン名 | △ | 「判定する」として実装（意味は同一） |

---

## 5. 結論

1. **Cursor Agent 用フォーマット（v1〜v3）は、段階的に「サイト全体 → 問題文・ヒント → 判定 UI」へと要件が具体化されている。**
2. **`typescript-intro-site` は、その流れに沿い、ブラウザ内 TS 実行・複数レッスン・ヒント・空の初期コード・判定ボタン・合格／不合格モーダル・不合格時のコードリセットを備えている。**
3. **第 1 版のみにあった庆贺／残念の文言は、第 3 版および現行実装では「合格！」／「不合格！」に統一されている。** 教材用コピーを厳密に v1 文言に合わせる場合は、モーダル文言の差し替えのみで対応可能。
4. **依頼フォーマットの「条件」「関連ファイル」は今後、パス・バリデーション・UI 制約を埋めると、Agent への指示がさらにブレなくなる。**

---

## 6. 参考：主要ファイル一覧（`typescript-intro-site`）

| パス | 役割 |
|------|------|
| `src/App.tsx` | 画面・状態・実行・判定・モーダル |
| `src/data/lessons.ts` | レッスン定義・期待出力 |
| `src/lib/runTypeScript.ts` | TS トランスパイルと実行 |
| `src/lib/normalizeOutput.ts` | 判定用出力の正規化 |
| `README.md` | 起動・ビルド手順 |

---

*作成日: 2026-03-22（リポジトリ上のファイル内容に基づく）*
