# 報告レポート_260423_01

## 対応依頼
`CursorAgentフォーマット_v1.md` の要件に沿って、既存アプリを改修・実装する。

## 実装方針
- 既存のフロントエンド単体構成を、`Frontend / Backend / DB` の3層構成へ分離する。
- バックエンドは `Node.js + Express + TypeScript`、DB は `PostgreSQL`、ORM は `Prisma` を採用する。
- API は `controller / service / repository` で責務分離し、レスポンス形式を統一する。
- ユーザごとのレッスン進捗を永続化し、初回アクセス時の進捗作成・進捗率更新・完了処理を実装する。

## 変更内容

### 1. バックエンド新設
- `typescript-intro-site/backend` を新規追加。
- `Express` サーバー起動構成を追加。
- 共通レスポンス型、エラーハンドリング、非同期ハンドラを追加。

### 2. DB/Prisma 実装
- `User`, `Lesson`, `Progress` モデルを定義。
- `Progress` に `status`, `progressRate`, `completedAt` を保持。
- `userId + lessonId` の一意制約を設定。
- seed スクリプトで初期ユーザ・初期レッスン・進捗初期データを投入可能にした。

### 3. API 実装
- ユーザCRUD: `/api/users`
- レッスンCRUD: `/api/lessons`
- 進捗取得: `/api/users/:userId/progress`
  - 初回アクセス時に進捗を自動作成
- 進捗更新: `PUT /api/users/:userId/progress/:lessonId`
- 進捗完了: `POST /api/users/:userId/progress/:lessonId/complete`

### 4. フロント改修
- レッスン情報・ユーザ情報・進捗情報を API から取得する構成に変更。
- ユーザ選択UIを追加。
- レッスンごとの進捗率とステータス表示を追加。
- 判定成功時に進捗完了APIを呼び出す処理を追加。
- 判定失敗時に進捗更新APIを呼び出す処理を追加。

## 主要変更ファイル
- `typescript-intro-site/backend/package.json`
- `typescript-intro-site/backend/prisma/schema.prisma`
- `typescript-intro-site/backend/prisma/seed.ts`
- `typescript-intro-site/backend/src/app.ts`
- `typescript-intro-site/backend/src/server.ts`
- `typescript-intro-site/backend/src/controllers/*`
- `typescript-intro-site/backend/src/services/*`
- `typescript-intro-site/backend/src/repositories/*`
- `typescript-intro-site/src/App.tsx`
- `typescript-intro-site/src/App.css`
- `typescript-intro-site/src/api/client.ts`
- `typescript-intro-site/src/api/learningApi.ts`

## 注意点
- 本環境では `npm` 実行確認を完了できないケースがあるため、実機で起動確認が必要。
- バックエンド起動前に `DATABASE_URL` 設定と Prisma の migrate/seed 実行が必要。
- `VITE_API_BASE_URL` 未設定時は `http://localhost:4000/api` を参照する構成。

## 実行確認（要実施）
- `backend` 側: `npm install` → `prisma migrate` → `prisma seed` → `npm run dev`
- `frontend` 側: `npm install` → `npm run dev`
- API疎通、進捗作成・更新・完了の動作確認
